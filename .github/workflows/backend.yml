name: Deploy Backend to Render
on:
  push:
    branches:
      - main
  paths:
    - 'server/**'
jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/Checkout@v4

      - name: Deploy to Render
        run: |
          curl -X POST "https://api.render.com/deploy/srv-d5tobji4d50c73eqicd0?key=heK_KecnOP4"
